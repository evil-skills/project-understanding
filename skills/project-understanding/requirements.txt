# Core dependencies for Project Understanding Interface
# Install with: pip install -r requirements.txt

# Parsing (optional but recommended)
tree-sitter>=0.21.0
# tree-sitter-languages and individual grammars are omitted due to Python 3.14 incompatibility
# Fallback logic in parser.py will handle extraction

# Build dependencies (required for compiling grammars)
setuptools>=65.0.0
wheel>=0.40.0

# Development dependencies (optional)
pytest>=7.0.0
pytest-cov>=4.0.0
ruff>=0.1.0
