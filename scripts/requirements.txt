# Project Understanding Skill - Pinned Dependencies
# Generated: 2026-01-30
# Python: >=3.10
#
# To install:
#   pip install -r scripts/requirements.txt
#
# To download for offline use:
#   pip download -r scripts/requirements.txt -d .pui/packages

# Core dependencies - tree-sitter for parsing
tree-sitter>=0.21.0
# tree-sitter-languages is omitted due to Python 3.14 incompatibility
# Fallback logic in parser.py will handle extraction

# Build dependencies (required for compiling grammars)
setuptools>=65.0.0
wheel>=0.40.0

# Development dependencies (optional)
pytest>=7.0.0
pytest-cov>=4.0.0
ruff>=0.1.0
